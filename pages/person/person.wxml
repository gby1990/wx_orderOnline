<!--pages/peoson/person.wxml-->

<view class="container">
<!-- 个人信息 -->
    <view class="userinfo">
        <image class="userinfo-avatar" src="{{userInfo.avatarUrl}}" />
        <text class="userinfo-nickname">{{userInfo.nickName}}</text>
    </view>
    <!-- 杂项 -->
    <view class="foodState">
        <view class="states">
            <image src="../../assets/icons/member.png" />
            <text>成为会员</text>
        </view>

        <view class="states">
              <image src="../../assets/icons/point.png" />
            <text>会员积分</text>
        </view>

        <view class="states">
             <image src="../../assets/icons/dingdan.png" />
            <text>我的订单</text>
        </view>
    </view>
    <!-- 功能列表 -->
    <view class="info_list">
        <view class="weui-cells">
            <block wx:for="{{userInfoList.first}}" wx:key = "index">
                <view class="weui-cell">
                    <view class="weui-cell__hd">
                        <image src="{{item.icon}}" />

                    </view>
                    <view class="weui-cell__bd">
                        {{item.text}}
                    </view>
                    <view class="weui-cell__ft">
                        <view class="weui-badge" wx:if="{{item.alreadRead}}">
                            {{item.unreadNum}}
                        </view>
                        {{item.mes}}

                    </view>
                </view>
            </block>
        </view>
        <view class="weui-cells">
            <block wx:for="{{userInfoList.second}}" wx:key = "index">
                <view class="weui-cell">
                    <view class="weui-cell__hd">
                        <image src="{{item.icon}}" />

                    </view>
                    <view class="weui-cell__bd">
                        {{item.text}}
                    </view>
                    <view class="weui-cell__ft">
                        {{item.mes}}
                        <view class="weui-badge" wx:if="{{item.alreadRead}}">
                            {{item.unreadNum}}
                        </view>
                    </view>
                </view>
            </block>
        </view>
        <view class="weui-cells">
            <block wx:for="{{userInfoList.third}}" wx:key = "index">
                <view class="weui-cell">
                    <view class="weui-cell__hd">
                        <image src="{{item.icon}}" />

                    </view>
                    <view class="weui-cell__bd">
                        {{item.text}}
                    </view>
                    <view class="weui-cell__ft">
                        {{item.mes}}
                        <view class="weui-badge" wx:if="{{item.alreadRead}}">
                            {{item.unreadNum}}
                        </view>
                    </view>
                </view>
            </block>
        </view>



    </view>

</view>